@model IEnumerable<Lista_spotkan>

@{
    ViewBag.Title = "Index";
}

<h2>Lista Spotkań</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Spotkanie.opis)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Uzytkownik.AspNetUsers.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Uzytkownik1.AspNetUsers.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Spotkanie.opis)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Uzytkownik.AspNetUsers.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Uzytkownik1.AspNetUsers.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
